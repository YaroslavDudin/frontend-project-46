import {
  describe, expect, it, test,
} from '@jest/globals';
import * as func from '../index.js';

describe('genDiff', () => {
  it('should be undefined', () => {
    expect(func.genDiff).toBeUndefined();
  });
});
describe('resolvePath', () => {
  it('should be undefined', () => {
    expect(func.resolvePath).toBeUndefined();
  });
})
